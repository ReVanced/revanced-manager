name: PR Build

on:
  workflow_dispatch:
    inputs:
      flutter-branch:
        required: true
        type: choice
        description: Flutter branch
        default: 'stable'
        options: 
        - stable
        - beta
        - dev
        - master
      app-variant:
        required: true
        type: choice
        description: App variant
        default: 'release'
        options: 
        - release
        - debug
        - profile
      pr-number:
        required: true
        description: PR number (No hashtag)
        
permissions:
  contents: read
  pull-requests: write

jobs:
  build:
    name: Build the application
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the PR
        env: 
          GH_TOKEN: ${{ github.token }}
        run: |
          gh repo clone ReVanced/revanced-manager
          cd revanced-manager
          gh repo set-default ReVanced/revanced-manager
          gh pr checkout ${{ inputs.pr-number }}
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Setup JDK
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'zulu'
          cache: gradle
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: ${{ inputs.flutter-branch }}
          cache: true
      - name: Install Flutter dependencies
        run: flutter pub get
      - name: Generate files with Builder
        run: flutter packages pub run build_runner build --delete-conflicting-outputs
      - name: Build with Flutter
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          flutter build apk --${{ inputs.app-variant }};
      - name: Set env
        run: |
          echo "COMMIT_HASH=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
      - name: Upload build
        uses: actions/upload-artifact@v3
        with:
          name: revanced-manager-${{ env.COMMIT_HASH }}-${{ inputs.pr-number }}-${{ inputs.app-variant }}-${{ inputs.flutter-branch }}
          path: | 
            build/app/outputs/flutter-apk/app-${{ inputs.app-variant }}.apk
            build/app/outputs/flutter-apk/app-${{ inputs.app-variant }}.apk.sha1
          
  notify-pull-request:
    name: Notify Pull Request
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Set env
        run: |
          echo "DATETIME=$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV
          if [[ "${{ needs.build.result }}" == "failure" ]]; then
            echo "MESSAGE=🚫 Build failed!" >> $GITHUB_ENV
          else
            echo "MESSAGE=✅ Build succeeded!" >> $GITHUB_ENV
          fi
      - name: "Comment to Pull Request #${{ inputs.pr-number }}"
        uses: thollander/actions-comment-pull-request@v2
        with:
          GITHUB_TOKEN: ${{ github.token }}
          pr_number: ${{ inputs.pr-number }}
          mode: recreate
          message: |
            ${{ env.MESSAGE }} @ ${{ env.DATETIME }}
            
            You can check the details [here](${{ github.event.workflow_run.html_url }}).
  
            - App variant: ${{ inputs.app-variant }}
            - Flutter branch: ${{ inputs.flutter-branch }}
